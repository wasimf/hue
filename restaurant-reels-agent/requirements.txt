anthropic>=1.0
pillow>=10.0
python-bidi>=0.4
requests>=2.31
imageio-ffmpeg>=0.5   # bundled ffmpeg binary fallback (system ffmpeg is preferred)
boto3>=1.34           # only needed when [storage] kind = "s3"
