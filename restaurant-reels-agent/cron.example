# Post every day at 12:30 (server local time) from whatever is in inbox/.
# crontab -e
30 12 * * * cd /path/to/restaurant-reels-agent && ANTHROPIC_API_KEY=... IG_USER_ID=... IG_ACCESS_TOKEN=... python3 -m reels_agent run >> state/cron.log 2>&1
